import { bgColor, borderRadius } from "../../variables/Variables";

const PatientInfo = () => {
	return (
		<section className="" style={{ backgroundColor: bgColor.bg_white, borderRadius: borderRadius.small }}>
			<div className="">Patient Info</div>
			<div className="">Get Appointment</div>
		</section>
	);
};

export default PatientInfo;
